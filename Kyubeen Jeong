gitc  clonehttps://github.com/KyubeenJeong/vercel-fastapi.git
from fastapi import FastAPI

app = FastAPI()

database = {}


@app.get("/items/{item_id}")
async def read_item(item_id: int):
    return {"item_id": item_id, "item_name": database.get(item_id, "Not found")}
